name: nd_logs
description: "A simple logging solution built entirely in Dart. Supports logging for web applications as well as others."
version: 0.0.2
homepage: https://github.com/uncoded-decimal/nd_logs

environment:
  sdk: ">=3.3.4 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:

platforms:
  android:
  ios:
  web:

topics:
  - logging
  - logs
  - user-logs
